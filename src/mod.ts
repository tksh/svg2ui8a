/**
 * Re-exports the two public functions and types from the svg2ui8a package.
 *
 * This file is hand-written (not generated). It surfaces the two subpath
 * imports from the root package import.
 */
export { svg2usvg } from "./usvg.ts";
export {
  usvg2rgba,
  // types are listed by name per constitution §4.2
} from "./rgba.ts";